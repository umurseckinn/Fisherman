import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.umurseckin.fishermansjourney',
  appName: 'Fishermans Journey',
  webDir: 'dist/public'
};

export default config;
